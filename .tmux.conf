# You probably already put this in
set -g prefix C-a
unbind-key C-b
bind-key C-a send-prefix

set -g set-Titles on
set -g set-Titles-string "#W"

bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R

bind '"' split-window -c "#{pane_current_path}"
bind % split-window -h -c "#{pane_current_path}"
bind c new-window -c "#{pane_current_path}"

bind z set-option mouse \; resize-pane -Z

setw -g mode-keys vi

bind-key -T copy-mode-vi WheelUpPane scroll-up
bind-key -T copy-mode-vi WheelDownPane scroll-down

bind-key -T copy-mode-vi v begin-selection
bind-key -T copy-mode-vi y copy-pipe "reattach-To-user-namespace pbcopy"
bind-key -T copy-mode-vi c copy-pipe "reattach-To-user-namespace pbcopy"

set-option -g history-limit 50000
set-option -g default-command "reattach-To-user-namespace -l bash"
set -g mouse on

